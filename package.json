{
  "name": "cap07b-repaso",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "dependencies": {
    "parcel": "^2.8.3"
  },
  "scripts": {
    "dev":"parcel serve ./src/index.html",
    "build": "parcel build ./src/index.html --dist-dir build --public-url /dwf-w5-parcel-server",
    "deploy": "gh-pages",
    "build-and-deploy": "npm run build && npm run deploy"

  }
}
